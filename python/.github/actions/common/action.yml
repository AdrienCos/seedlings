---
name: "Common"
description: "Common steps for all Actions"
runs:
  using: "composite"
  steps:
    - uses: actions/setup-python@v7
      with:
        python-version: "3.14"
    - uses: astral-sh/setup-uv@v10.0.1
    - run: uv sync
      shell: bash
